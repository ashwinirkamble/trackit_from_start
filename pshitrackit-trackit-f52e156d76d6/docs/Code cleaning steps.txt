Code Clean Up

-------------------------------------------------------------------------------

Models
* add extend BaseModel
* delete inherited methods

-------------------------------------------------------------------------------

* Find and replace: "[\s]*/{2,}[\s]*end (of )*[a-z-A-Z-]+"
e.g. // end of if
-------------------------------------------------------------------------------

* Find and replace: " [\r\n]{2}[\s]{4}/[\*]{3,}[\s\n\r]+\* Function: [a-zA-Z]+[\s\n\r]+[\*]{3,}/"

For removing stuff like this:
    /****************************************************************************
     * Function: getPtoTravelList
     ****************************************************************************/

-------------------------------------------------------------------------------
  In JSP Files:
-------------------------------------------------------------------------------

Remove these comments
* "\s+<\!-- (end )*tan_table -->"

Reformat these:
* "<bean:write name="([a-zA-Z]+)"/>" to "\$\{$1\}"

* "<bean:write name="([a-zA-Z]+)" property="([a-zA-Z]+)"/>" to "\$\{$1\.$2\}"

-------------------------------------------------------------------------------

Remove author

\s\*\s+\* @author   [a-zA-Z]+\s[a-zA-Z]+[\r\n]+ \* @version  [0-9\./\s,]+[\n\r]+ \* @since    [A-Za-z0-9\./\s,]+[\n\r]+
